import 'package:flutter/material.dart';

class CharactersViews extends StatelessWidget {
  const CharactersViews({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("KArakterler"));
  }
}
